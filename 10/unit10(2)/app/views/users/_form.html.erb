<%= form_for(@user) do |f| %>
<table width="600">
    <tr><td colspan="2">
      <% if @user.errors.any? %>
        <div id="error_explanation">
          <h2>您输入的数据存在<%= pluralize(@user.errors.count, "") %> 处错误：</h2>
          <ul>
          <% @user.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
    </td></tr>
    <tr><td>姓名：</td><td>
      <div class="field"><%= f.text_field :name %><br>请填写您的真实姓名</div>
    </td></tr>
    <tr><td>确认姓名：</td><td>
      <div class="field"><%= f.text_field :name_confirmation %></div>
    </td></tr>
    <tr><td>出生年份：</td><td>
      <div class="field"><%= f.text_field :year %><br>请输您的四位出生年份（1910-2011之间）</div>
    </td></tr>
    <tr><td>性别：</td><td>
      <div class="field"><%= f.select(:sex,options_for_select(["","男","女"])) %></div>
    </td></tr>
    <tr><td>地址：</td><td>
      <div class="field"><%= f.text_field :address %></div>
    </td></tr>
    <tr><td>联系电话：</td><td>
      <div class="field"><%= f.text_field :phone %><br>固话或手机，如0371-66666666或13888888888</div>
    </td></tr>
    <tr><td colspan="2">
      <div class="actions"><%= f.submit("下一步") %></div>
    </td></tr>
</table>
<% end %>
