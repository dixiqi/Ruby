
<div id="fl">
	<table>
        <thead>
          <tr>
            <th>编号</th>
            <th>标题</th>
            <th>链接地址</th>
            <th>排序编号</th>
            <th>是否显示</th>
            <th></th>
          </tr>
        </thead>
      <tbody>
        <% @friend_links.each do |friend_link| %>
          <tr>
            <td><%= friend_link.id %></td>
            <td><%= friend_link.title %></td>
            <td><%= friend_link.url %></td>
            <td><%= friend_link.taxis %></td>
            <td><%= friend_link.isShow %></td>
            <td>
              <%= link_to '编辑', edit_friend_link_path(friend_link) %>
          </tr>
        <% end %>
      </tbody>
</table>

<br />

<%= link_to '添加友情链接', new_friend_link_path %>
</div>